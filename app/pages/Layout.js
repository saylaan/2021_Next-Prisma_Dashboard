import React, { useEffect, useState } from 'react';
import Sidebar from "./Sidebar";

const Layout = props => {
    return (
        <div>
            <Sidebar />
        </div>
    );
};

export default Layout;